# INFER backend local overrides (copy to .env)
HOST=127.0.0.1
PORT=8000
# Comma-separated list for the external frontend origin(s)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000
# Absolute or relative path for IFC / derived artifacts (on-prem only)
# DATA_DIR=C:\Users\Isaac Chia\INFER\data
